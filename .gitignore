# --- OS cruft ---
Thumbs.db
desktop.ini
ehthumbs.db
.DS_Store

# --- Editors ---
.vscode/
.idea/
*.swp
*~

# --- Node (test dependencies only; the app itself needs no install) ---
node_modules/
package-lock.json
npm-debug.log*

# --- Local scratch ---
*.log
tmp/
_publishing-guide.md

# NOTE: data/*.js are generated by tools/mirror.py but ARE committed on purpose —
# they are what makes the app work when index.html is opened by double-click.
